## Phase 4

- ~~Battery status indication (charge level and status)~~ **built, then removed.**
  The 18650 and its holder are off the board. See "Every GPS start is a cold
  start" in `docs/hardware-notes.md` for why the coin cell does not replace it.

## Phase 5

- ~~Sleep mode: gps and display off, sleep by button hold, wake by the same
  button~~ **built, then removed.** With no cell fitted, "asleep" and
  "unplugged" are the same state. The triple click it used now switches the
  display between the GPS and CAN screens.

## Phase 6

-
